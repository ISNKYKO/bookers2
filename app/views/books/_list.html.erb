
      <h2>Books</h2>
  <table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <% books.each do |book| %>
  <tbody>
   <tr>
   <td><%= image_tag book.user.get_profile_image(100,100) %><br>
   <%= book.user.name %>
   </td>
   <td><%= link_to "#{book.title}", books_path(book) %></tb>
   <td><%= book.body %></td>
   <% end %>
   </tr>
  </tbody>
  </table>
  </div>

